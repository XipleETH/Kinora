# 6FPS Devvit App
# This app uses Reddit's native Media API for image storage.
# No external storage configuration is needed — images are uploaded
# directly to Reddit's CDN (i.redd.it) via the media.upload() API.
